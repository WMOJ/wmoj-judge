// Always answers "1 2", whatever the case. Right for one checker case and
// wrong for the other -- which is the point: the checker, not the bytes,
// decides.
#include <cstdio>

int main() {
  std::printf("1 2\n");
  return 0;
}
